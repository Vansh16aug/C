#include <stdio.h>
#include<string.h>
int main()
{
    // char str[100];
    // printf("Enter a string: ");
    // scanf("%s", str);
    //predefined string
    char str[] = "Hello";
    //length of string
    // int i=0;
    // while(str[i]!='\0'){
    //     i++;
    // }   
    // printf("Length of string is %d",i);
    //length of string using functions
    // int len = strlen(str);
    // printf("Length of string is %d",len);
    printf("%s",str[0]);
    return 0;
}