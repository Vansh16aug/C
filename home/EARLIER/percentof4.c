#include <stdio.h>
int main()
{
    float m1=40;
    float m2=39;
    float m3=29;
    float m4=39;
    float p = (m1 + m2 + m3 + m4) / 160 * 100;
    printf("The percentage of 4 subjects : %f",p);
    return 0;
}